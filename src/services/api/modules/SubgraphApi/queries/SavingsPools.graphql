subscription SavingsPools {
  savingsPools(first: 1000) {
    id
    poolToken {
      id
      name
      symbol
      decimals
    }
    tokens {
      id
      symbol
      decimals
    }
    aprHistory(first: 10) {
      amount
      duration
    }
  }
}
